# Figure briefs — Chapter 18 — What will last

Briefs for the figures of `manuscript/ch18-what-will-last.md`, held here rather than in the chapter so the prose reads clean.
Each brief follows `FIGURES.md` §6. The caption and alt-text in the chapter are generated with the brief and must be changed here and there together.

## Figure 18.1 — Durable principles versus volatile tooling

```
FIGURE BRIEF
- id:            Figure 18.1
- title:         Two layers, two clocks
- type:          architecture
- claim:         The book separates a slow-moving layer of principles from a fast-moving layer of tooling, and assigns each to the medium that suits its clock: print for principles, repository for tooling.
- standfirst:    What turns over in months goes to the repository; what holds goes to print.
- canvas:        16:9
- elements:      a horizontal divider splitting the canvas into two bands; upper band
                 "volatile — tooling" holding four grey tags: "model versions", "prices",
                 "protocols", "benchmarks", with a fast-clock glyph; lower band
                 "durable — principles" (grey structural border) holding four near-black
                 tags: "instrument stance", "specification", "verification",
                 "accountability", with a slow-clock glyph; two medium markers at the
                 right, "repository" and "print"
- flow:          no directional flow; vertical stacking carries the contrast
- labels:        "volatile — tooling", "model versions", "prices", "protocols",
                 "benchmarks", "durable — principles", "instrument stance",
                 "specification", "verification", "accountability", "print", "repository"
- annotations:   on the upper band, "turns over in months — faster than any publishing
                 cycle"; beside the upper band's marker, "repository: versioned, dated,
                 corrected between releases"; on the lower band, "derives from how
                 science treats any instrument — it predates these models and will
                 outlast them"; beside the lower band's marker, "print: the position and
                 the reasoning"; a footer, "read the reasoning in print, fetch the
                 current detail from the repository — neither is complete alone"
- caption:       Figure 18.1 — Two layers, two clocks, two media. The tooling layer turns over in months, so it lives in the repository, where an entry can carry a date and be corrected between releases. The principles layer derives from how science treats any instrument, so it lives in print. The book only works if you read it as one instrument with two clocks: the reasoning here, the current detail there.
- alt-text:      Two stacked bands split by a horizontal divider. The upper band, volatile tooling, holds four grey tags, model versions, prices, protocols and benchmarks, beside a fast clock, annotated turns over in months, and assigned to the repository, which is versioned and dated. The lower band, durable principles, holds four near-black tags, the instrument stance, specification, verification and accountability, beside a slow clock, annotated derives from how science treats any instrument, and assigned to print. A footer reads read the reasoning in print, fetch the current detail from the repository, and expect neither to be complete alone.
- infographic description: A flat vector two-band diagram, 16:9, off-white background.
                 Title top-left: "Two layers, two clocks". Standfirst: "What turns over
                 in months goes to the repository; what holds goes to print." A
                 horizontal divider splits the canvas. Upper band labelled "volatile —
                 tooling", holding four grey tags in a row: "model versions", "prices",
                 "protocols", "benchmarks"; a small fast-clock glyph beside the label;
                 band annotated "turns over in months — faster than any publishing
                 cycle"; at its right edge a marker "repository" annotated "versioned,
                 dated, corrected between releases". Lower band labelled "durable —
                 principles", bordered grey, holding four near-black tags: "instrument
                 stance", "specification", "verification", "accountability"; a slow-clock
                 glyph beside the label; band annotated "derives from how science treats
                 any instrument — it predates these models and will outlast them"; at its
                 right edge a marker "print" annotated "the position and the reasoning".
                 Footer: "read the reasoning in print, fetch the current detail from the
                 repository — neither is complete alone". Sentence case throughout.
```
